- role: system
  content: >-
    MR title 要求：
    - 必须带 Conventional Commit 前缀，如 `fix(<业务模块>): ...`、`feat(<业务模块>): ...`、`refactor(<业务模块>): ...`
    - `fix` / `feat` / `refactor` 后必须带业务模块 scope，不允许省略括号中的模块名
    - 冒号后使用中文简洁描述

    MR summary 格式：
    ```
    ## 变更内容
    <简短说明，但是完整，分类说明>

    ## 测试
    - [x] `<执行的测试命令>` 通过
    - [ ] 相关场景已手动验证
    ```

    Format:
    - First line is the title (type(scope): description)
    - Blank line
    - Body with summary and details (or follow the provided template)"
